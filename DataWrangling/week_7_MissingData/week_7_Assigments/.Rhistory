df %>%
group_by(age) %>%
summarise_all(
n()
)
df %>%
group_by(age) %>%
summarise_all(
list(min,max)
)
df %>%
group_by(age) %>%
summarise_all(
n = sum()
)
df %>%
group_by(age) %>%
summarise_all(
n = sum(x)
)
df %>%
group_by(age) %>%
summarise_all(
n = ()
df %>%
group_by(age) %>%
summarise_all(
n()
)
df %>%
group_by(age) %>%
summarise_all(
n(x)
)
df %>%
group_by(age) %>%
summarise_all(
n = n()
)
df %>%
group_by(age) %>%
summarise_all(
n = n()
)
df %>%
group_by(age) %>%
summarise_all(
mean = mean(df)
)
df %>%
group_by(age) %>%
summarise_all(
nmissing = colnames(is.na(df)),
perc_col = colSums(is.na(df))/nrow(df)*100
)
df %>%
group_by(age) %>%
summarise_all(
nmissing = colnames(is.na(df)),
perc_col = colSums(is.na(df))/nrow(df)*100
)
df %>%
group_by(age) %>%
summarise(
nmissing = colnames(is.na(df)),
perc_col = colSums(is.na(df))/nrow(df)*100
)
df %>%
group_by(age) %>%
summarise(
#nmissing = colnames(is.na(df)),
perc_col = colSums(is.na(df))/nrow(df)*100
)
df %>%
group_by(age) %>%
summarise(
nmissing = colnames(is.na(df)),
perc_col = colSums(is.na(df))/nrow(df)*100
)
df %>%
group_by(age) %>%
summarise(
nmissing = colnames(is.na()),
perc_col = colSums(is.na())/nrow()*100
)
df %>%
group_by(age) %>%
summarise_all(
nmissing = colnames(is.na()),
perc_col = colSums(is.na())/nrow()*100
)
df %>%
group_by(age) %>%
summarise_all(
nmissing = colnames(is.na()),
perc_col = colSums(is.na())/nrow()*100
)
df %>%
group_by(age) %>%
summarise_all(
nmissing = colnames(is.na()),
perc_col = colSums(is.na())/nrow()*100
)
df %>%
group_by(age) %>%
summarise_all(
x = sum()
)
df %>%
group_by(age) %>%
summarise_all(
x = sum()
)
df %>%
group_by(age) %>%
summarise_all(
x = sum(df)
)
visdat::vis_dat(df)
df %>%
group_by(age) %>%
summarise_all(
n = n()
)
df %>%
group_by(age) %>%
summarise_all(
n = n(df)
)
df %>%
group_by(age) %>%
summarise_all(
n = sum(df)
)
df %>%
group_by(age) %>%
summarise_all(
n = sum(df)
)
df %>%
group_by(age) %>%
)
df %>%
group_by(age) %>%
df %>%
group_by(age) %>%
df
df %>%
group_by(age) %>%
summarise()
df
df %>%
group_by(age) %>%
summarise(bmi)
df
df %>%
group_by(age) %>%
summarise()
df
df %>%
group_by(age)%>%
summarise_all(function(x) sum(is.na(x))/n()*100)%>%
round(2)
md.pattern(fgds)
load(fgds)
md.pattern(fdgs)
md.pattern
md.pattern(fdgs)
# mean( df$wgt) and mean(df$hgt).
#Assign the fgds to a another df (dosent lose information later)
fdgs_nomiss <- fdgs
fdgs_nomiss %>%
mutate(
wgt = if_else(is.na(wgt), mean(wgt, ra.rm = TRUE), wgt)
)
fdgs_nomiss
md.pattern(fdgs_nomiss)
# mean( df$wgt) and mean(df$hgt).
#Assign the fgds to a another df (dosent lose information later)
fdgs_nomiss <- fdgs
fdgs_nomiss %>%
mutate(
wgt = if_else(is.na(wgt), mean(wgt), wgt)
)
fdgs_nomiss
md.pattern(fdgs_nomiss)
# mean( df$wgt) and mean(df$hgt).
#Assign the fgds to a another df (dosent lose information later)
fdgs_nomiss <- fdgs
fdgs_nomiss %>%
mutate(
wgt = if_else(is.na(wgt), mean(wgt), wgt)
)
fdgs_nomiss
md.pattern(fdgs_nomiss)
# mean( df$wgt) and mean(df$hgt).
#Assign the fgds to a another df (dosent lose information later)
fdgs_nomiss <- fdgs
fdgs_nomiss %>%
mutate(
wgt = if_else(is.na(wgt), mean(wgt, na.rm = TRUE), wgt)
)
fdgs_nomiss
md.pattern(fdgs_nomiss)
# mean( df$wgt) and mean(df$hgt).
#Assign the fgds to a another df (dosent lose information later)
fdgs_nomiss <- fdgs
fdgs_nomiss %>%
replace(
wgt = if_else(is.na(wgt), mean(wgt, na.rm = TRUE), wgt)
)
# mean( df$wgt) and mean(df$hgt).
#Assign the fgds to a another df (dosent lose information later)
fdgs_nomiss <- fdgs
fdgs_nomiss %>%
replace_na(
wgt = if_else(is.na(wgt), mean(wgt, na.rm = TRUE), wgt)
)
# mean( df$wgt) and mean(df$hgt).
#Assign the fgds to a another df (dosent lose information later)
fdgs_nomiss <- fdgs
fdgs_nomiss %>%
replace_na(wgt, mean(wgt, na.rm = TRUE))
# mean( df$wgt) and mean(df$hgt).
#Assign the fgds to a another df (dosent lose information later)
fdgs_nomiss <- fdgs
fdgs_nomiss %>%
replace_na(wgt, mean(wgt, na.rm = TRUE))
# mean( df$wgt) and mean(df$hgt).
#Assign the fgds to a another df (dosent lose information later)
fdgs_nomiss <- fdgs
within(data = fdgs_nomiss,
expr =
{
wgt <- replace(x = wgt,
list = is.na(x = wgt),
values = mean(x = wgt, na.rm = TRUE) )
}
)
fdgs_nomiss
md.pattern(fdgs_nomiss)
# mean( df$wgt) and mean(df$hgt).
#Assign the fgds to a another df (dosent lose information later)
fdgs_nomiss <- fdgs
within(data = fdgs_nomiss,
expr =
{
wgt <- replace(x = wgt,
list = is.na(x = wgt),
values = mean(x = wgt, na.rm = TRUE) )
}
)
is.na(fdgs_nomiss)
# mean( df$wgt) and mean(df$hgt).
#Assign the fgds to a another df (dosent lose information later)
fdgs_nomiss <- fdgs
within(data = fdgs_nomiss,
expr =
{
wgt <- replace(x = wgt,
list = is.na(x = wgt),
values = mean(x = wgt, na.rm = TRUE) )
}
)
colSums(is.na(fdgs_nomiss))
# mean( df$wgt) and mean(df$hgt).
#Assign the fgds to a another df (dosent lose information later)
fdgs_nomiss <- fdgs
fdgs_nomiss <- within(data = fdgs_nomiss,
expr =
{
wgt <- replace(x = wgt,
list = is.na(x = wgt),
values = mean(x = wgt, na.rm = TRUE) )
}
)
colSums(is.na(fdgs_nomiss))
md.pattern(fdgs_nomiss)
data <- fdgs[, c("age", "sex", "wgt")]
imp <- mice(data, method = "norm.predict", seed = 1,
m = 1, print = FALSE)
xyplot(imp, ~ wgt)
data <- fdgs[, c("age", "sex", "wgt")]
imp <- mice(data, method = "norm.predict", seed = 1,
m = 1, print = FALSE)
xyplot(imp, data ~ wgt)
data <- fdgs[, c("age", "sex", "wgt")]
imp <- mice(fdgs, method = "norm.predict", seed = 1,
m = 1, print = FALSE)
xyplot(imp, age ~ wgt)
data <- fdgs[, c("age", "sex", "wgt")]
imp <- mice(fdgs, method = "norm.predict", seed = 1,
m = 1, print = FALSE)
xyplot(imp, wgt ~ age)
#data <- fdgs[, c("age", "sex", "wgt")]
imp <- mice(fdgs, method = "norm.predict", seed = 1,
m = 1, print = FALSE)
xyplot(imp, wgt ~ age)
#data <- fdgs[, c("age", "sex", "wgt")]
imp <- mice(fdgs, method = "norm.predict", seed = 1,
m = 1, print = FALSE)
xyplot(imp, sex ~ age)
#data <- fdgs[, c("age", "sex", "wgt")]
imp <- mice(fdgs, method = "norm.predict", seed = 1,
m = 1, print = FALSE)
xyplot(imp, wgt ~ age)
ggplot(imp, aes(x=sex, y=wgt, color=sex)) +
geom_point()
#data <- fdgs[, c("age", "sex", "wgt")]
imp <- mice(fdgs, method = "norm.predict", seed = 1,
m = 1, print = FALSE)
#xyplot(imp, wgt ~ age)
ggplot(imp, aes(x=sex, y=wgt, color=sex)) +
geom_point()
#data <- fdgs[, c("age", "sex", "wgt")]
imp <- mice(fdgs, method = "norm.predict", seed = 1,
m = 1, print = FALSE)
#xyplot(imp, wgt ~ age)
ggplot(fgds, aes(x=sex, y=wgt, color=sex)) +
geom_point()
#data <- fdgs[, c("age", "sex", "wgt")]
imp <- mice(fdgs, method = "norm.predict", seed = 1,
m = 1, print = FALSE)
#xyplot(imp, wgt ~ age)
ggplot(fdgs, aes(x=sex, y=wgt, color=sex)) +
geom_point()
#data <- fdgs[, c("age", "sex", "wgt")]
imp <- mice(fdgs, method = "norm.predict", seed = 1,
m = 1, print = FALSE)
#xyplot(imp, wgt ~ age)
ggplot(fdgs, aes(x=age, y=wgt, color=sex)) +
geom_point()
#In fact it makes, so lets run a regression
imp <- mice(fdgs, method = "norm.predict", seed = 1,
m = 1, print = FALSE)
xyplot(imp, wgt ~ age)
summary(fdgs_nomiss)
summary(imp)
#building new df
#Assign the fgds to a another df (dosent lose information later)
fdgs_nomiss <- fdgs
fdgs_nomiss <- within(data = fdgs_nomiss,
expr =
{
wgt <- replace(x = wgt,
list = is.na(x = wgt),
values = imp
}
#building new df
#Assign the fgds to a another df (dosent lose information later)
fdgs_nomiss2 <- fdgs
fdgs_nomiss2 <- within(data = fdgs_nomiss,
expr =
{
wgt <- replace(x = wgt,
list = is.na(x = wgt),
values = mean(x = imp, na.rm = TRUE) )
}
)
#CHECK FINAL RESULT
colSums(is.na(fdgs_nomiss2))
#building new df
#Assign the fgds to a another df (dosent lose information later)
fdgs_nomiss2 <- fdgs
fdgs_nomiss2 <- within(data = fdgs_nomiss,
expr =
{
wgt <- replace(x = wgt,
list = is.na(x = wgt),
values = mean(x = imp, na.rm = TRUE) )
}
)
#CHECK FINAL RESULT
fdgs_nomiss2
#building new df
#Assign the fgds to a another df (dosent lose information later)
fdgs_nomiss2 <- fdgs
fdgs_nomiss2 <- within(data = fdgs_nomiss,
expr =
{
wgt <- replace(x = wgt,
list = is.na(x = wgt),
values = mean(x = imp, na.rm = TRUE) )
}
)
#CHECK FINAL RESULT
fdgs_nomiss2
colsSum(is.na(fdgs_nomiss2))
#CHECK FINAL RESULT
colSum(is.na(fdgs_nomiss2))
#CHECK FINAL RESULT
colSums(is.na(fdgs_nomiss2))
#building new df
#Assign the fgds to a another df (dosent lose information later)
fdgs_nomiss2 <- fdgs
fdgs_nomiss2 <- within(data = fdgs_nomiss2,
expr =
{
wgt <- replace(x = wgt,
list = is.na(x = wgt),
values = mean(x = imp, na.rm = TRUE) )
}
)
#CHECK FINAL RESULT
colSums(is.na(fdgs_nomiss2))
#building new df
#Assign the fgds to a another df (dosent lose information later)
fdgs_nomiss2 <- fdgs
fdgs_nomiss2 <- within(data = fdgs_nomiss2,
expr =
{
wgt <- replace(x = wgt,
list = is.na(x = wgt),
values = (x = imp ) )
}
)
#building new df
#Assign the fgds to a another df (dosent lose information later)
data_imp <- complete(imp)
mean(data_imp$wgt)
#CHECK FINAL RESULT
colSums(is.na(data_imp))
summary(data_imp)
# mean( df$wgt) and mean(df$hgt).
#Assign the fgds to a another df (dosent lose information later)
fdgs_nomiss <- fdgs
fdgs_nomiss <- within(data = fdgs_nomiss,
expr =
{
wgt <- replace(x = wgt,
list = is.na(x = wgt),
values = mean(x = wgt, na.rm = TRUE) )
}
)
#CHECK FINAL RESULT
colSums(is.na(fdgs_nomiss))
#Comparison between methods
summary(fdgs_nomiss)
var(data_imp)
#Look in Standard deviation and Variance:
print(sd(data_imp))
#Look in Standard deviation and Variance:
sd(fdgs_nomiss)
sd(data_imp)
sd(fdgs_nomiss$wgt)
sd(data_imp$wgt)
#Look in Standard deviation and Variance:
print(sd(fdgs_nomiss$wgt))
print(sd(data_imp$wgt))
#Look in Standard deviation and Variance:
print(sd(fdgs$wgt)) #original df
print(sd(fdgs_nomiss$wgt)) #using mean
print(sd(data_imp$wgt)) #using regression
print(sd(fdgs$wgt, na.rm = TRUE)) #original df
#Look in Standard deviation and Variance:
print(sd(fdgs$wgt, na.rm = TRUE)) #original df
print(sd(fdgs_nomiss$wgt)) #using mean
print(sd(data_imp$wgt)) #using regression
#Look in Standard deviation
print(sd(fdgs$wgt, na.rm = TRUE)) #original df
print(sd(fdgs_nomiss$wgt)) #using mean
print(sd(data_imp$wgt)) #using regression
print(var(fdgs$wgt, na.rm = TRUE)) #original df
print(var(fdgs_nomiss$wgt)) #using mean
print(var(data_imp$wgt)) #using regression
#Look in Standard deviation
sd(fdgs$wgt, na.rm = TRUE) #original df
sd(fdgs_nomiss$wgt) #using mean
sd(data_imp$wgt) #using regression
var(fdgs$wgt, na.rm = TRUE) #original df
var(fdgs_nomiss$wgt) #using mean
var(data_imp$wgt) #using regression
#Standard deviation
sd(fdgs$wgt, na.rm = TRUE) #original df
sd(fdgs_nomiss$wgt) #using mean
sd(data_imp$wgt) #using regression
#Variance
var(fdgs$wgt, na.rm = TRUE) #original df
var(fdgs_nomiss$wgt) #using mean
var(data_imp$wgt) #using regression
# mean( df$wgt) and mean(df$hgt).
#Assign the fgds to a another df (dosent lose information later)
fdgs_nomiss <- fdgs
fdgs_nomiss <- mice(fdgs_nomiss, method = "mean", m = 1, maxit = 1)
#CHECK FINAL RESULT
colSums(is.na(fdgs_nomiss))
# mean( df$wgt) and mean(df$hgt).
#Assign the fgds to a another df (dosent lose information later)
fdgs_nomiss <- fdgs
fdgs_nomiss <- mice(fdgs, method = "mean", m = 1, maxit = 1)
#CHECK FINAL RESULT
colSums(is.na(fdgs_nomiss))
# mean( df$wgt) and mean(df$hgt).
#Assign the fgds to a another df (dosent lose information later)
imp_mean <- mice(fdgs, method = "mean", m = 1, maxit = 1)
fdgs_nomiss <- complete(imp_mean)
#CHECK FINAL RESULT
colSums(is.na(fdgs_nomiss))
md.pattern(fdgs_nomiss)
# Lookinf visual if makes sense this correlation:
ggplot(fdgs, aes(x=age, y=wgt, color=sex)) +
geom_point()
#Comparison between methods (only on weight) => Try to see the diference in the mean( 0.380 vs 0.385)
summary(fdgs_nomiss)
#Standard deviation
sd(fdgs$wgt, na.rm = TRUE) #original df
sd(fdgs_nomiss$wgt) #using mean
sd(data_imp$wgt) #using regression
#Variance
var(fdgs$wgt, na.rm = TRUE) #original df
var(fdgs_nomiss$wgt) #using mean
var(data_imp$wgt) #using regression
